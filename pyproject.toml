[project]
name = "datalake"
version = "2.0.0"
description = "Personal data lake for Claude Code conversations, voice typing, and email"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.0.0",
    "elasticsearch>=8.11.0",
    "claude-agent-sdk>=0.1.19",
    "anthropic>=0.40.0",
]

[project.scripts]
claude-parser = "parsers.claude_parser:main"
voice-parser = "parsers.voice_parser:main"
chatgpt-parser = "parsers.chatgpt_parser:main"
datalake-web = "web.app:main"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
